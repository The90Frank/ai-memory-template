---
name: identity
type: user
scope: global
created: 2026-08-29
last_used: 2026-08-29
uses: 1
confidence: high
pin: true
tags: [git, identity]
---

# Identity

> 📌 **Fork checklist** — placeholders below. Fill them in, then delete this quote block.

| Field | Value |
|---|---|
| GitHub handle | `<YOUR-HANDLE>` |
| Personal email | `<you@example.com>` |
| Personal repo host | GitHub — `github.com/<YOUR-HANDLE>` |
| Language | chat and code, commits and PRs — see [[global]] |

These are the values to use **on GitHub**, and for creating personal remote repos. ⚠️ **They are not the universal git identity**: that is chosen by the host, see [[git-identity]].

## This machine

`[TO BE FILLED IN on the machine's branch]` — OS, home, shell, where the work lives, branch name.

🔑 **Machine facts do not live on `main`**: they differ from machine to machine, and `main` is the common base every branch is born from. They are written on your own branch — see [[machine-branches]].

Declared constraint: the setup must stay **portable across Linux, Windows and macOS** in the same way.

See also [[git-identity]] · [[response-style]] · [[commit-conventions]] · [[memory-architecture]]
